const hello = (nama: String) => {
    return "Hello " + nama;
}

const encrypt = (nama: String) => {
    //lakukan encrypt 
    let hasilEncrypt = "do something ";
    return hasilEncrypt;
}

const validasiForm = (nama: String) => {
    //lakukan encrypt 
    let hasilEncrypt = "lakukan validasi form";
    return hasilEncrypt;
}

export const useNama = {
    hello: hello,
    encrypt: encrypt,
    validasiForm: validasiForm,
}